@model IEnumerable<TechReady.Portal.Models.AudienceType>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TypeOfAudience)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TypeOfAudience)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.AudienceTypeID }) |
            @Html.ActionLink("Details", "Details", new { id=item.AudienceTypeID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.AudienceTypeID })
        </td>
    </tr>
}

</table>
