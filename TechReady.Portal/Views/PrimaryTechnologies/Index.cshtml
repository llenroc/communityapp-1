@model IEnumerable<TechReady.Portal.Models.PrimaryTechnology>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.PrimaryTech)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.PrimaryTech)
        </td>
        <td>
            @*@Html.ActionLink("Edit", "Edit", new { id=item.PrimaryTechnologyID }) |*@
            @Html.ActionLink("Details", "Details", new { id=item.PrimaryTechnologyID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.PrimaryTechnologyID })
        </td>
    </tr>
}

</table>
